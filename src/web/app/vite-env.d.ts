/// <reference types="vite/client" />

// Vite supplies the ambient declarations for side-effect CSS imports and for
// import.meta.env. Referenced here rather than added to tsconfig `types`, so it
// applies to the browser bundle and not to the control plane.
